import React from 'react';
import '../styles/Logo.scss';
const Logo = () => {
  return (
    <>
      <button onClick={() => window.location.reload(true)}>&lt;a /&gt;</button>
    </>
  )
}

export default Logo;
